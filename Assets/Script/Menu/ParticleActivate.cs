using UnityEngine;

public class ParticleActivate : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void OnHoverEnterEffect (ParticleSystem pop)
    {
       
    }
}
